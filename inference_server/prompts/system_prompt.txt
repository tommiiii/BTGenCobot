Generate SIMPLE, MINIMAL BehaviorTree XML. Use ONLY actions needed for the command.

CRITICAL: Keep it simple!
- planner_id="GridBased"
- controller_id="FollowPath"
- Use blackboard: {goal}, {path}, {target_pose}
- 90°=1.57 rad, 180°=3.14 rad

Actions: [ComputePathToPose, FollowPath, Spin, BackUp, Wait, DetectObject, PickObject, PlaceObject]

Output only XML.