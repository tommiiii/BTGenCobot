# Goal: Command to generate and execute a BehaviorTree
string command              # Natural language command (e.g., "Pick up the red cup")
---
# Result: Outcome of the generation and execution
bool success                # Overall success (true if BT was generated and executed)
string bt_xml_path          # Path to the generated BT XML file
string error_message        # Error description if failed (empty if success)
---
# Feedback: Progress updates during execution
string status               # Current phase: "generating" | "validating" | "executing" | "completed"
float32 progress            # Progress from 0.0 to 1.0
string current_step         # Description of current step
